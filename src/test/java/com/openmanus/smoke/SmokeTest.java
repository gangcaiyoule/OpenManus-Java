package com.openmanus.smoke;

import org.junit.jupiter.api.Tag;

/**
 * Smoke tests tag interface.
 * Smoke tests verify core functionality works correctly.
 * They run quickly and fail fast to catch critical issues early.
 */
@Tag("smoke")
public interface SmokeTest {
}
